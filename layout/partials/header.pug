- var avatar = theme.avatar
.header
  .avatar
    a(href = url_for('/'))
      img(src = avatar)
  if theme.enableSocialIcon
    .social
      each social in theme.socials
        if social.link
          a(href = social.link)
            i(class=`fab fa-${social.name}`)
