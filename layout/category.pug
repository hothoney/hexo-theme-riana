extends partials/layout.pug

block content
  #category
    h2= page.category + ':'
    each item in page.posts.toArray()
      include mixins.pug
      +make_post_link
  include partials/paginator.pug